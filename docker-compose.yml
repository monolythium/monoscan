services:
  monoscan:
    build:
      context: .
    ports:
      - "${MONOSCAN_PORT:-8080}:8080"
    environment:
      PORT: 8080
      MONOSCAN_RPC_UPSTREAM: ${MONOSCAN_RPC_UPSTREAM:-https://rpc.monolythium.com}
    restart: unless-stopped
